# Project Memories — Index

Shared, checked-in project memory. Auto-loading is not guaranteed for this folder (Claude Code's auto-memory reads from `~/.claude/projects/<hash>/memory/`), but `CLAUDE.md` at the repo root references this folder so future sessions know to consult it for project-scoped context.

When adding a new memory, append a one-line pointer below. Before adding, check that the fact isn't already in `CLAUDE.md`, in `docs/<module>/goal.md`, or derivable from the code — those are not memory material.

## Project facts

- [project-overview.md](project-overview.md) — Four modules: analog_mc + data_pipelines + forecasters + gbdt (all v1 shipped to main as of 2026-05-26). Per-module `docs/<module>/goal.md` for specifics
- [project-data-source.md](project-data-source.md) — analog_mc reads from `data/NASDAQ100.csv`; gbdt v1 reads NIFTY 50 panel via `data_pipelines.fetch()`; per-module switchover for analog_mc is a separate plan
- [project-streamlit.md](project-streamlit.md) — Local dashboard designed for concurrent runs (per-run lockfile, no global mutex)
- [project-results-layout.md](project-results-layout.md) — Aggregated experiment JSONs live in `results/<module>/data/`, separated from `docs/<module>/` narratives
- [project-fat-tail-eval.md](project-fat-tail-eval.md) — Operational how-to for the mandatory 15-anchor fat-tail eval panel (scripts, fig paths, regeneration rules)
- [project-nse-data-quirks.md](project-nse-data-quirks.md) — NSE fetch quirks: jugaad/nselib blocked → use curl from archives.nseindia.com; filter DUMMY* placeholders; processed.db-wal can corrupt → /tmp/exp_data workaround; per-ticker fetch is 30–80s; cached-but-short tickers need `back_extend=True`
- [project-gbdt-uniqueness-weights.md](project-gbdt-uniqueness-weights.md) — gbdt applies LdP §4.4 sample-uniqueness weights by default; corrects the 2.15× overlap-prevalence inflation surfaced by sweep exp #1; opt-out via `target.uniqueness_weighting: false`
- [project-r-precision-methodology.md](project-r-precision-methodology.md) — R-precision (per-day variable K=R(d)) is the headline cross-cell metric for gbdt; AUC alone misclassifies top-tail-signal cells as null; compound rule + compute_r_precision.py script
- [project-gbdt-tuning-playbook.md](project-gbdt-tuning-playbook.md) — diagnostic-first FS+HP tuning rules (nifty50 H=25 study): check train/val gap before pruning; HP-ceiling detection; monotone constraints neutral-to-harmful on interaction-driven cells (check 1D PDP not marginal corr); importance≈0 = redundant not unrelated
- [project-xgboost-interaction-analysis.md](project-xgboost-interaction-analysis.md) — XGBoost native TreeSHAP (`pred_contribs`/`pred_interactions`) for feature-interaction analysis: method, O(rows×feat²) cost mitigations, drop-only-if-low-main-AND-low-interaction pruning rule, GPU-OK-for-diagnostics-not-training split; grounds V1.2 (#165)
- [project-xgboost-training-essentials.md](project-xgboost-training-essentials.md) — XGBoost backend training contract: load-bearing DETERMINISM pins (seed + `tree_method=exact` + `n_jobs=1` + cpu, hard-fail) for the finalization-retrain; HP-name mapping vs CatBoost (no `has_time`); built-in L1/L2 + missing-value handling; calibration unchanged; portability
- [project-agent-loop-wrapper.md](project-agent-loop-wrapper.md) — `scripts/gbdt/run_agent_loop_resumable.sh` is the standard launcher for single long-running gbdt experiments + agent-loop runs (≥30 min): setsid detachment + `--resume` auto-restart on crash (when checkpoint exists) + heartbeat-stall watchdog + idempotency guard + atomic 7-state status JSON at `<out-dir>/.wrapper/status.json` (`starting | running | restarting | heartbeat_stalled_killed | exited_ok | exited_failed | max_retries_hit`). Honest caveats: can't recover from a mid-cold-features-build death (no checkpoint exists yet); auto-restart amplifies environmental failures (OOM, disk wedge) — set `--max-retries 0` until root-cause fixed. Sweep scripts call `gbdt.experiment` directly today; wrapper-in-sweep is a possible follow-up
- [project-gbdt-cache-invalidation.md](project-gbdt-cache-invalidation.md) — gbdt feature cache (per-cell + universe, both atomic) invalidates iff `src/gbdt/features.py` source changes; post-#190 schema v2 dropped `code_commit`/`code_dirty` for `source_sha256`; unrelated module edits keep the cache warm. Foot-guns: assumes `gbdt.features` is sole derivation source; edits to `feature_code_signature()` itself need a manual `SCHEMA_VERSION` bump; `black`/`ruff`/rebase touching features.py invalidates everywhere

## Workflow feedback

- [feedback-branch-retention.md](feedback-branch-retention.md) — Don't propose deleting merged plan/feature branches; history is wanted
- [feedback-experiment-agent-loop.md](feedback-experiment-agent-loop.md) — Bake loop-pattern guidance (background shell + Monitor filter + ScheduleWakeup + parallel-worker w/ per-item timeout) into sub-agent launch prompts up front; SendMessage isn't exposed
- [feedback-track-agent-tasks.md](feedback-track-agent-tasks.md) — Every sub-agent launch → immediate TaskCreate + status=in_progress; mark completed only when the work is actually closed out
- [feedback-disk-wedge-pattern.md](feedback-disk-wedge-pattern.md) — NTFS-on-Linux volumes can wedge when near-full → kernel D-state procs, unkillable, cascading; pre-flight disk check + cleanup via paths off the wedged FS
- [feedback-sub-agent-foreground.md](feedback-sub-agent-foreground.md) — Sub-30-min sub-agent tasks: run in FOREGROUND with `timeout`, NOT background+Monitor (the latter exits prematurely, orphans procs); ≥2h tasks still use background+Monitor+ScheduleWakeup chain
- [feedback-worktree-symlink-contract.md](feedback-worktree-symlink-contract.md) — Sub-agents can't `git worktree add` (sandbox blocks); parent pre-creates. Symlink: `rm -rf data && ln -s /mnt/122CEE982CEE765F/cache_data data` (scratch cache, NOT main checkout's `data/` — main's `processed.db-wal` is corrupt per `project-nse-data-quirks` § 3). NOT `ln -snf` — creates nested `data/data`. Also `ln -sf <main>/.env .env`. Verify with `readlink data` + `sqlite3 ... 'PRAGMA quick_check'`. Then `git update-index --skip-worktree data/.gitkeep` so the shadowed tracked file doesn't break `git rebase --autostash` / `git checkout`
- [feedback-agent-pkill-antipattern.md](feedback-agent-pkill-antipattern.md) — Sub-agents MUST NOT use `pkill -f <pattern>` in bash wrappers; pattern-based kills catch other concurrent processes (cost us 2 sp500 H=25 retries 2026-05-27). Use direct PID targeting or plain `timeout(1)` instead
- [feedback-task-priority-graph.md](feedback-task-priority-graph.md) — Project convention: when optimizing/prioritizing tasks, ALWAYS render the dependency graph to PNG (+SVG) via local graphviz `dot` (mermaid-cli unavailable) and display it via SendUserFile — automatically, not just inline text. Styling + recipe inside
- [feedback-subagent-transcript-parsing.md](feedback-subagent-transcript-parsing.md) — To report a background sub-agent's progress, parse its `*.output` JSONL with a bounded python/jq/grep query (extract counts + recent tools + truncated last text); the "do not read" harness reminder guards against dumping the whole file, not inspecting it. Reusable recipe inside
- [feedback-process-death-verification.md](feedback-process-death-verification.md) — Before declaring a long-running child process dead and relaunching it, verify it's actually dead. Primary recipe: `kill -0 $(cat <out-dir>/.wrapper/pid)` if launched through the wrapper (post-#193 path; pre-#193 installs wrote `<out-dir>/wrapper.pid` at top level — fall back to that legacy path for older cells), otherwise `ps -ef | grep -E '<exact-command-pattern>'` (NOT `pgrep -af gbdt` — over-matches sibling cells in sweeps). Sub-agent session termination is NOT a reliable signal that the child died (the cascading-SIGHUP-from-sub-agent-death theory is weaker than initially suspected; check `dmesg`/`journalctl -k` before settling on a cause). Avoids the duplicate-launch foot-gun. If verified alive: do NOT touch the cell. Wrapper's idempotency guard only catches double-launches THROUGH the wrapper — raw `setsid` relaunches still need this verification
- [feedback-auto-fire-review-merge.md](feedback-auto-fire-review-merge.md) — Default: immediately fire the autonomous review/merge background agent for every PR the assistant opens via `gh pr create`; do NOT pre-ask per PR. CLAUDE.md § Environment carries the headline; this memory holds the *why* (user directive 2026-05-27) + the exception list (HOLD-for-user for CLAUDE.md / memories / SKILL.md / spec; new dependencies/modules; substantive content concerns). Non-negotiables still apply: `--rebase` not `--squash`, never `--delete-branch`, `gh pr comment` not `gh pr review --approve`, no AI attribution
- [feedback-hourly-wakeup-mandate.md](feedback-hourly-wakeup-mandate.md) — Hourly cron #94 fires a brief health-check + triage pass and then ACTs (does not stall on user decisions per 2026-05-28 directive). Mandatory: wall-clock time at top via `date '+%Y-%m-%d %H:%M %Z'`; best-cell-so-far line for any running sweep per `[[feedback-sweep-best-cell-reporting]]`; cache health-checks go against the scratch cache (`--data-root /mnt/122CEE982CEE765F/cache_data`); auto-fire review/merge sub-agent on stale assistant-authored PRs (except HOLD-for-user); "all green" + stop if nothing actionable. Crons auto-expire after 7 days — re-arm weekly
- [feedback-rebase-merge.md](feedback-rebase-merge.md) — Merge PRs with `gh pr merge <num> --rebase` (rebase-and-merge), NEVER `--squash`. Standing user directive 2026-05-28 (PR #61 approval); codified in CLAUDE.md via PR #63. Method directive ONLY — not a blanket merge authorization (HOLD-for-user categories still wait for user). Applies to direct merges AND every sub-agent brief. Pair with `[[feedback-branch-retention]]` (never `--delete-branch`) and the gh-author-self-approval block (use `gh pr comment`)
- [feedback-sweep-best-cell-reporting.md](feedback-sweep-best-cell-reporting.md) — Sweep status reports (hourly check, completion notification, ad-hoc) MUST surface the best-cell-so-far by weighted R-precision lift on test, citing the cell name + raw numbers + base rate, not just `N/M done`. User asked for this 2026-05-31 during parallel r1k+nasdaq sweeps. "0 done — no best yet" if cold; one line per running sweep in multi-sweep status. Applies across cron, sweep-agent reports, dependency graphs
