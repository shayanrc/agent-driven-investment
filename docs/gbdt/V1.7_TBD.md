# gbdt V1.7 — TBD (deferred follow-ups)

Parking lot for work discovered during V1.7 (`V1.7_fundamentals_features_plan.md`
+ the `_272` fundamentals A/B). Promote to a real plan when a coherent slice is big.

## Gating step before any fundamentals promotion

1. **[DONE 2026-07-04] Second-window validation — split verdict (`_273`).** Re-ran
   the matched-HP A/B on the **trailing** window (test 2026-02→04/05, non-overlapping
   with `_272`'s 2024-H2). Result: **+50%/50d replicates robustly** (AUC↑ + test Brier↓ +
   R-p@1/@10/@20↑ on both windows; R-p@3/@5 dip −0.01 on date-aligned but recover
   +0.12–0.15 on trailing — net-positive R-p curve on both — stronger than macro ever
   was, which failed its second window) but **+20%/25d does NOT** (only R-p@1, R-p@5
   + Brier stay positive on both; AUC gain and R-p@3/@10/@20 were window-specific). So promotion is **per-champion**: F18 is
   validated for +50%/50d only. Next gate for that cell → §3 (per-arm tune) before
   any `/daily-predictions` wiring; a champion swap stays a human decision (`_019`).

## Robustness

2. **Defensive duplicate-`fiscal_period_end` guard in `valuation.ttm.build_ttm_timeline`.**
   (Carried from `docs/valuation/V2_TBD.md` §1, relevant because F18 consumes the
   valuation panel.) The TTM consecutiveness check keys on a `>110`-day gap, so a
   *duplicate* fiscal_period_end (0-day gap) would slip through and could double-count
   a quarter. Can't happen today (us_fundamentals is PK'd by grid date), but a
   defensive drop-duplicates/assert would harden it before the panel is trusted for
   deployment.

## Feature reach (only if the gate passes)

3. **[NOW ACTIONABLE — +50%/50d] Per-arm FS/HP tuning of the fundamentals model.**
   `_272`/`_273` are single-fit matched-HP A/Bs (clean isolation of the +9 columns).
   +50%/50d passed the two-window gate, so the next step for that cell is a full
   agent-driven FS+HP tune of the `all_fundamentals` arm — measure the tuned ceiling
   vs the deployed champion's config (the A/B measured the *marginal feature*
   effect, not the *tuned* ceiling). **Note:** F18 grew to **13 columns** after
   `_272`/`_273` (added `fund_<yield>_xs_zscore` + `fund_rev_ttm_yoy_xs_zscore` — the
   cross-sectional z-scores parallel to the ranks). `_272`/`_273` validated the 9-col
   precursor; this tune is where the full 13-col set gets its first evaluation, so
   the FS step can select among ranks vs z-scores per cell. **`_274` makes this
   doubly-motivated:** the 17-cell horizon×target sweep found the 13-col F18 at forced
   single-fit is *neutral* on +50%/50d (AUC −0.005, R-p@10 −0.022) — weaker than `_272`'s
   9-col result — because the added z-scores help long-horizon cells but dilute this one.
   The FS step should drop the diluting z-scores and recover/exceed the 9-col edge; that's
   the concrete thing the tune must demonstrate.

5. **[parked] Two-window confirmation of the long-horizon F18 winners.** `_274` found the
   F18 edge is strongest in the **long-horizon corner** (+40%/200d — everything up; +50%/200d,
   +40%/100d — top-of-book), none of which are tracked. Before treating any as a candidate
   cell, run the same `_272`→`_273` trailing-window confirmation on the top 2–3 long-horizon
   winners. Candidate-cell input only — a champion swap is a human decision (`_019`).
   **`_275` strengthens the case:** the tuned +40%/200d fund model reaches R-p@1 0.58 /
   R-p@3 0.46 (base 0.119) — the strongest fundamentals cell on record. The trailing
   confirmation should test the tuned config (141-feat FS + l2 4.5), not just single-fit.

4. **More fundamentals features.** F18 is now 13 columns (yields + xs-rank + xs-zscore
   + rev-YoY + earnings-yield change). Candidates if the family proves out: margin
   trend (net-income/revenue), FCF-margin, P/FCF and PS *levels* (not just yields),
   accruals, sequential (QoQ) growth, and balance-sheet ratios once
   `us_fundamentals` gains balance-sheet columns (`data_pipelines` V3_TBD §2).
