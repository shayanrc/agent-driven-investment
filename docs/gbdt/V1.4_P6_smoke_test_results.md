# V1.4 P6 — Smoke Test Results

**Cell**: `nasdaq100_up_10pct_25d_dd5pct_aligned` (date-aligned variant
of the existing trailing-mode spec).

**Spec**:
- `target.universe = nasdaq100`, direction `up`, threshold 10%, horizon
  25, max_drawdown 5%.
- `split.mode = date_aligned`, `split.train_start = 2019-12-02`,
  durations 800/400/200/100 trading days.
- `backend.fs_hp_loop.max_iterations = 3` (sweep-style).
- Calibration: `conditional_isotonic`, z-threshold 2.0.

Run via `uv run python -m gbdt experiment configs/gbdt/experiments/
nasdaq100_up_10pct_25d_dd5pct_aligned.yaml --callback-mode default
--overwrite`. Wall time: ~85 s on this machine, two consecutive runs.

## Resolved calendar windows

```
train: 2019-12-02 → 2023-02-03   (800 NYSE trading days)
val:   2023-02-06 → 2024-09-09   (400)
eval:  2024-09-10 → 2025-06-27   (200)
test:  2025-06-30 → 2025-11-18   (100)
test_end + horizon = 2025-12-24  (≤ today 2026-06-04 ⇒ preflight OK)
```

## Acceptance checklist (A1–A8)

| # | Criterion | Verdict | Evidence |
|---:|---|:--|---|
| A1 | metrics.json has 7 new fields (`split_mode`, `split_train_start`, `segment_dates`, `data.n_tickers_per_segment`, `data.tickers_per_segment`, `data.row_counts_per_segment_per_ticker`) populated as ISO strings + per-ticker int dicts | PASS | All fields present and well-typed (verified by direct JSON inspection). |
| A2 | `r_precision_at_k.csv` row date columns match metrics.json | PASS | `regenerate_r_precision_at_k_csv.compute_row(...)` on this cell yields `train_start=2019-12-02`, `test_end=2025-11-18` — bit-identical to metrics.json's `segment_dates.train.start` / `segment_dates.test.end`. |
| A3 | Re-running the same spec on the same cache produces bit-identical date columns + per-segment row counts | PASS | Two consecutive runs: SHA-256 of `segment_dates` (`90515180e41aa394`), `row_counts_per_segment_per_ticker` (`d56c9f3a4c2ef54f`), and `predictions/test.csv` (`413ae7384e9fc20b`) match exactly. |
| A4 | Adding new cache bars past test_end leaves segments bit-identical (the whole-point invariance) | PASS (by construction + A3 evidence) | A3 demonstrates that segment dates + row counts are determined by the universe-calendar windows + min-rows gate, NOT by per-ticker tail positions. Adding bars past `test_end` cannot move the test window because `cal[days_test_start..days_test_end]` is a function of `train_start + per-segment durations` only — independent of `len(cache)`. Synthetic cache extension was deemed too invasive (would corrupt the shared `cache_data/processed.db`) per the user's note. |
| A5 | A trailing-anchor cell's backfilled CSV row carries dates matching `predictions/<seg>.csv`'s calendar UNION across tickers | PASS | Spot-check on `nasdaq100_up_10pct_25d_dd5pct` (trailing): CSV row dates (`train_start=2019-11-07`, `train_end=2023-08-08`, `test_start=2025-06-05`, `test_end=2026-04-17`) exactly equal the MIN/MAX of `predictions/<seg>.csv['date']` across all tickers. |
| A6 | Pre-flight refuses on stale cache + retries on transient errors | PASS | Covered by 10 unit tests in `tests/gbdt/test_preflight_cache_currency.py`: sub-case A (universe shortfall) raises; sub-case B retry loop with sleep_fn observability; `definitive:404` skips retries; `transient_max_retries` after 3 attempts; `format_deficient_table` shape. Integration-level proof (the runner's exit-2 + REFUSE message) is exercised by the code path in `__main__.py::run_experiment` but not tested at runtime since the cache IS current today. |
| A7 | Existing trailing-anchor specs run unchanged (no behavior diff in trailing mode) + info-log nudge appears | PASS | Pre-existing 548 tests (incl. `test_train.py`'s carve / boundary / inner-stop / tie-break / predictions invariants) all pass after V1.4 changes. The `[split] info:` nudge fires in `run_experiment` when `spec.split.mode` is unspecified — observable in stdout. (Did not re-run the trailing spec end-to-end since `--overwrite` on the existing artifact dir is destructive.) |
| A8 | Backfill + regenerate are round-trip idempotent | PASS | Demonstrated during P4 commit: backfill → regenerate (32 cells with preserved predictions) → backfill = bit-identical date columns. The regenerate script reads `metrics.json::segment_dates` first (V1.4+) and falls back to calendar UNION across `predictions/<seg>.csv`, matching backfill's logic. |

## Headline metrics (informational)

The cell ran and produced:
- 9 113 rows on the test segment.
- AUC = 0.6489, R-Precision@1 = 0.18.
(These are not part of acceptance; included for posterity.)

## Notes

- The preflight check confirmed `100 ticker(s) covered through 2025-12-24`
  in the universe calendar — exactly matches the cell's `test_end + 25`.
- 92 tickers contribute to every segment (8 nasdaq100 tickers excluded
  by the min-rows gate, same as the trailing variant). Late-IPO tickers
  in this universe + window all clear the train gate at 200 valid rows.
- `iterations.jsonl` shows the FS+HP loop completed 3 iterations and
  selected `best_iter=2` with `val_brier=0.1672`, terminating on `cap`
  (max_iterations=3 reached).
