# gbdt V1.5 — TBD / parking lot

Follow-ups discovered while landing V1.5-era work that are out of scope for the current PRs. Promote a row to its own plan doc + branch when a coherent slice becomes "a project, not a chore."

| # | Item | Why parked | Promotion gate |
|---:|---|---|---|
| 1 | **CatBoost agent-tune vs CatBoost sweep on H=200.** `_268` decomposed the `_267` shortfall and found the backend dominates — CatBoost is far more robust at H=200 (lowest eval→test decay, best test top-1/@3 in all 3 cells), while the agent's eval-R-p@1 loop on **XGBoost** is high-variance (rescued a pathological XGBoost-default on sp500, over-fit a good one on russell). The 2×2 left one cell unfilled: **CatBoost + full agent_file_protocol**. Run it (≥1 cell, e.g. russell1000 +40%/200d where XGBoost-agent failed worst) to test whether tuning the *robust* backend can beat the cheap CatBoost sweep — the natural "can we beat the bar" experiment. | The `_268` investigation question ("why does agent < sweep") is fully answered by Phases 1–2 (vintage rejected, backend dominant, eval-objective unreliable). CatBoost-agent answers a *forward* question (can tuning improve on the sweep), not the diagnostic one; and it's a heavy agent loop (~1–1.5 h/cell). | A decision to push H=200 top-K skill past the CatBoost-sweep baseline — file a plan, run CatBoost-agent on the 3 cells, compare to `*_aligned_resnap`. |
| 2 | **Loop objective: eval R-p@1 is a poor single-window target at H=200.** `_268` showed eval R-p@1 and test R-p@1 are weakly/negatively coupled at long horizons (russell: agent had the *highest* eval @1 but a low test @1). The agent loop, blind to test, over-fits the single eval window. Consider a **longer / multi-window eval** (e.g. rolling eval blocks) or a **decay-penalized objective** (penalize eval→holdout gap) so the loop selects HP that generalize. | Affects only long-horizon (H≥100) agent-tuning; short-horizon champions (`_019` cadence) are unaffected and CatBoost sidesteps it. Needs runner work (multi-window eval scoring + finalizer change). | A long-horizon agent-tuning push where CatBoost isn't an option — promote to a runner plan. |

---

**Promotion process.** Pick a row, draft its own `V1.<N>_<scope>_plan.md` doc on its own branch, file the task, open the plan-review PR. Don't stream V1.5-scoped commits to main — per CLAUDE.md "Plans and branches" the rule is per-plan, not per-change.

**Last reviewed**: 2026-06-23 (created — rows from `_268` agent-vs-sweep decomposition).
