# gbdt V1.4 — TBD / parking lot

Follow-ups discovered while planning + landing V1.4 work (`V1.4_date_aligned_splits_plan.md`, `V1.4_228_H100_rerun_plan.md`) that are out of scope for the current V1.4 PRs. Promote a row to its own plan doc + branch when a coherent slice becomes "a project, not a chore."

| # | Item | Why parked | Promotion gate |
|---:|---|---|---|
| 1 | **Canonical CSV regenerate drops rows for unreachable predictions.** The `scripts/gbdt/regenerate_r_precision_at_k_csv.py` script walks `predictions/test.csv` across reachable worktrees and recomputes from scratch. The #228 regenerate dropped **29 legacy rows** whose `predictions/test.csv` was either renamed to an `_agentloop`-suffixed variant (already present + freshly recomputed) or lives on the two I/O-error worktrees `wt-exp-midcap150-up50-100d` / `wt-exp-nifty500-up30-50d` that the script skips. Options: (a) re-materialize the dropped predictions from saved models, or (b) freeze the legacy R-p values into a separate "archive" CSV so the regenerate script doesn't keep silently dropping them. See #228 memo `docs/gbdt/_228_h100_rerun.md` § Methodology notes. | Pre-existing hygiene gap surfaced by #228; not blocking long-horizon coverage in the canonical CSV. A separate decision on the right archival contract. | Either (a) the corrupted worktrees get resurrected and the dropped rows return on the next regenerate, or (b) the team decides legacy rows deserve a frozen archive entry — file a plan to implement it. |
| 2 | **V1.4 D2 anchor table needs a horizon-aware entry (#233).** Canonical D2 says `train_start = 2019-01-01`, but H=200 specs in #228 could not run with that anchor (train + val + eval + test + horizon overflowed the per-ticker window from 2019); the H=200 cells were pinned to `train_start = 2018-01-01`. The D2 anchor table currently records only 2019-01-01. | #228 is the first long-horizon sweep under V1.4; the issue surfaced only at H=200. | #233 (V1.4 D2 horizon-aware anchor) lands. |

---

**Promotion process.** Pick a row, draft its own `V1.<N>_<scope>_plan.md` doc on its own branch, file the task, open the plan-review PR. Don't stream V1.4-scoped commits to main — per CLAUDE.md "Plans and branches" the rule is per-plan, not per-change.

**Last reviewed**: 2026-06-05 (initial creation alongside #228 H≥100 rerun memo).
