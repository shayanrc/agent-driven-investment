# gbdt V1.4 — TBD / parking lot

Follow-ups discovered while planning + landing V1.4 work (`V1.4_date_aligned_splits_plan.md`, `V1.4_228_H100_rerun_plan.md`) that are out of scope for the current V1.4 PRs. Promote a row to its own plan doc + branch when a coherent slice becomes "a project, not a chore."

| # | Item | Why parked | Promotion gate |
|---:|---|---|---|
| 1 | **Canonical CSV regenerate drops rows for unreachable predictions.** The `scripts/gbdt/regenerate_r_precision_at_k_csv.py` script walks `predictions/test.csv` across reachable worktrees and recomputes from scratch. The #228 regenerate dropped **29 legacy rows** whose `predictions/test.csv` was either renamed to an `_agentloop`-suffixed variant (already present + freshly recomputed) or lives on the two I/O-error worktrees `wt-exp-midcap150-up50-100d` / `wt-exp-nifty500-up30-50d` that the script skips. Options: (a) re-materialize the dropped predictions from saved models, or (b) freeze the legacy R-p values into a separate "archive" CSV so the regenerate script doesn't keep silently dropping them. See #228 memo `docs/gbdt/_228_h100_rerun.md` § Methodology notes. | Pre-existing hygiene gap surfaced by #228; not blocking long-horizon coverage in the canonical CSV. A separate decision on the right archival contract. | Either (a) the corrupted worktrees get resurrected and the dropped rows return on the next regenerate, or (b) the team decides legacy rows deserve a frozen archive entry — file a plan to implement it. |
| 2 | **V1.4 D2 anchor table needs a horizon-aware entry (#233).** Canonical D2 says `train_start = 2019-01-01`, but H=200 specs in #228 could not run with that anchor (train + val + eval + test + horizon overflowed the per-ticker window from 2019); the H=200 cells were pinned to `train_start = 2018-01-01`. The D2 anchor table currently records only 2019-01-01. | #228 is the first long-horizon sweep under V1.4; the issue surfaced only at H=200. | #233 (V1.4 D2 horizon-aware anchor) lands. |
| 3 | ~~**Parallelize the feature build (`src/gbdt/features.py`) — it's single-core.**~~ **DONE → `docs/gbdt/V1.5_feature_build_perf_plan.md`** (branch `gbdt-feature-build-parallel`). Profiled first: the real cost was Python-bound (F16 streak loop + its per-ticker stitching = 56%, the two rolling-rank `apply`s = 33%), NOT the C-rolling families. **Vectorized** all three (rolling `rank(method='max', pct=True)`; F16 stitching → `_per_ticker` groupby; streak loop → numpy run-length) — **bit-identical (max\|Δ\|=0, all 279 cols, full slice) and 3.84× faster (294s→77s)**. Process parallelism (option a) proved unnecessary; the `_019` `--since` slice already covered option (c). | ~~The experiment runner amortizes via the matrix cache, so it only bites the fresh-inference/forward-OOS path — which is now a recurring cadence (`ROLLING` node). Not blocking; just slow.~~ | **RESOLVED** — promoted + landed as V1.5 after `_019` made forward-OOS a daily cadence. |
| 4 | **`backfill_csv_segment_dates.py` has two footguns surfaced while landing the stray-date fix (`_dense_date_bounds`, DONE — branch `gbdt-segment-dates-stray-fix`).** Re-running the *wholesale* backfill on the current checkout (a) **destructively blanks** the 8 date columns for any registry row whose artifact dir is found but has no reachable `predictions/*.csv` (e.g. predictions lived on a now-pruned worktree) — losing previously-correct dates; and (b) writes `n_iterations_run` as `1.000000` where the regenerate writes `1`, so the two canonical writers disagree on number formatting. Because of (a)+(b), the stray-date correction was applied **main-checkout-only + non-blanking** (only overwrite a date cell when a real dense bound is derived) rather than via a wholesale backfill run. | Pre-existing footguns in the *secondary* writer; the canonical refresh is the regenerate (which carries unreachable rows rather than blanking, and the hardened `_dense_date_bounds` is now in both writers). Not blocking. | Guard the backfill to (a) skip empty derivations (keep the existing value) and (b) match the regenerate's int formatting for `n_iterations_run`; or deprecate the backfill in favour of the regenerate. |

---

**Promotion process.** Pick a row, draft its own `V1.<N>_<scope>_plan.md` doc on its own branch, file the task, open the plan-review PR. Don't stream V1.4-scoped commits to main — per CLAUDE.md "Plans and branches" the rule is per-plan, not per-change.

**Last reviewed**: 2026-06-29 (added row 4 — `backfill_csv_segment_dates.py` blanking + formatting footguns, surfaced while landing the `_dense_date_bounds` stray-date fix).
