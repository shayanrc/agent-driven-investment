# gbdt V1.3 — TBD / parking lot

Follow-ups discovered while planning V1.3 Option A (`V1.3_option_a_loop_anti_auc_integration_plan.md`) that are out of scope for the Option A PR. Promote a row to its own plan doc + branch when a coherent slice becomes "a project, not a chore."

| # | Item | Why parked | Promotion gate |
|---:|---|---|---|
| 1 | **Option B — scout response-curve phase before iter_0.** Parallel single-knob fits to map the val_brier × eval R-p@K surface in one pass, seed iter_0 from scout winners (playbook rule 11 stage 2 codified into the runner). | ~300 lines + spec keys + bundle changes; the value is highest after Option A validates that per-iter eval R-p@K is the right oracle signal. | Option A merges + cell-5 A4 acceptance passes (test R-p@1 ≥ 0.6714 via the loop). |
| 2 | **Tiny-model HP envelope as a spec-selectable preset.** Current `hp_starting` defaults are CatBoost-shaped (depth=6, lr=0.05, ~1000 iters). Add an XGBoost "tiny" preset (depth=2, eta=0.1, ES=30, nest=500) the spec can opt into, plus auto-suggestion in the iter_0 bundle when `anti_auc_flag == "true"`. | The cell-5 winner is in a regime the loop's defaults can't reach. Option A surfaces eval R-p@K so the agent CAN navigate there from scratch, but a preset shortcut is faster. | Option A acceptance — if A4 stretch (test R-p@1 ≥ 0.75) fails because the agent can't get to the tiny-model regime in ≤16 iters, this becomes the next phase. |
| 3 | **Per-iter val R-p@K.** Currently we're adding *eval* R-p@K (rule 10: val R-p@K is too noisy). If a future cell shows val R-p@K is useful (large Q_days × multiple picks/day), additive. | Q_days × 1 pick/day is too small for val R-p@K to be reliable per _211 F2. Adding it costs negligibly but the agent's prompt would need to handle "trust eval, ignore val" → "trust both" carefully. | A cell where val R-p@K is empirically stable across iter trajectories (signal-to-noise > 3:1, say). |
| 4 | **Sweep CSV staleness self-check.** Option A looks up `results/gbdt/data/r_precision_at_k.csv` at iter_0 for the anti-AUC flag. If the CSV is stale (cell ran but row not yet appended), the flag is "unknown" — agent loses the signal. | Acceptable for v1.3 (the CSV is hand-maintained and updates per-memo-PR). A self-refresh path (e.g. "regenerate the row from the on-disk artifact if missing") would close the gap but adds machinery that's unclear is worth the cost. | A cell where the agent would have benefitted from the flag but the CSV row was missing. |
| 5 | **L1 tie-break general audit (#215).** Option A auto-disables L1 only on anti-AUC cells. Whether L1 is correct on the other cell shapes is a separate empirical question — if #215 concludes L1 is wrong elsewhere too, V1.4 may remove it entirely. | Need data across enough cell shapes (not just one anti-AUC cell) to call the question. | #214 (cells 1+3 replication) lands + #215 audit completes. |
| 6 | **Eval prediction caching across `--resume`.** Per-iter eval predict is < 1s on hist+nj=8, but over a 16-iter loop = ~16s. Caching the eval segment indexing/feature-pull across iters saves ~14s of duplicate work. | Low-priority optimization; doesn't change behavior. | Empirical: > 5% of total run wall-clock is in eval predict. Almost certainly never on hist+nj=8 cells; potentially on a CatBoost run. |
| 7 | **Anti-AUC flag override via spec.** Currently the flag is computed from the canonical CSV lookup; no manual override. A user re-running cell-5 might want to force `anti_auc_flag: false` to test what the un-doctrine-modified loop does on that cell. | Cleaner to ship with no override (the canonical CSV is the source of truth); the override is needed only for ablation experiments. | An ablation study that requires the override. |
| 8 | **Multi-cell sweep integration with eval R-p@K.** When the sweep runner (V2 territory per goal.md) lands, the per-cell sweep output should include eval R-p@K alongside val_brier so cross-cell comparisons can use the right metric. | Sweep runner doesn't exist yet (parked in `V1.1_TBD.md`). When it lands, this is a natural addition. | Sweep runner promotion. |

---

**Promotion process.** Pick a row, draft its own `V1.<N>_<scope>_plan.md` doc on its own branch, file the task, open the plan-review PR. Don't stream V1.3-scoped commits to main — per CLAUDE.md "Plans and branches" the rule is per-plan, not per-change.

**Last reviewed**: 2026-06-02 (initial creation with Option A plan).
