"""Inference: load a saved per-(fold, target) artifact and score new feature rows.

Implementation lands in V1_PLAN Stage 6 (used internally by the walk-forward
driver) and Stage 8 (CLI surface).
"""
