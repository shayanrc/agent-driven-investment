"""CSV loader for the gbdt module.

CSV-first contract per ``[[project-data-source]]`` — same shape as
``analog_mc.data``. Asset-agnostic: column names come from config, not
hardcoded. Implementation lands in V1_PLAN Stage 1.
"""
