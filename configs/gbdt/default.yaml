# gbdt — v1 global defaults
#
# Per-experiment specs in configs/gbdt/experiments/<name>.yaml override
# only what they care about; everything else falls through to this file.
#
# Schema reference: docs/gbdt/EXPERIMENT_SPEC.md.
# Architecture: docs/gbdt/V1_PLAN.md.
# HP semantics: docs/gbdt/CATBOOST_HP_REFERENCE.md.

# ---------------------------------------------------------------------------
# Universe presets. v1 pre-registers nifty50 + the broader NSE universes
# (nifty100, nifty_midcap_150, nifty500) and the US universes (sp500,
# nasdaq100, russell1000). Inline ad-hoc baskets are registered on first
# use by the /gbdt-experiment skill's universe self-service flow — see
# docs/gbdt/V1_PLAN.md Stage 1 § "Universe self-service" and
# .claude/skills/gbdt-experiment/SKILL.md § "Pre-flight".
#
# An unknown universe is a hard error in gbdt.data.universe_metadata —
# no silent fallback to NIFTY-style defaults. See PR #100 for the why:
# Exp 2 (nifty100 +10%/20d) computed every macro feature against NIFTY:50
# instead of NIFTY:100 because the runner masked a missing block.
#
# Schema (see docs/data_pipelines/universe_yaml_spec.md for the full spec):
#   source                — repo-relative path to the constituent YAML.
#   index_ticker          — benchmark identifier in the data_pipelines cache;
#                           drives F1/F5/F9/F9b macro features.
#   annualization_factor  — trading days per year (NSE=250, US=252);
#                           propagated into the feature builder.
# ---------------------------------------------------------------------------
universes:
  nifty50:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty50.yaml
    index_ticker: "NIFTY:50"
    annualization_factor: 250
  nifty100:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty100.yaml
    index_ticker: "NIFTY:100"
    annualization_factor: 250
  nifty_midcap_150:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_midcap_150.yaml
    index_ticker: "NIFTY:MIDCAP150"
    annualization_factor: 250
  nifty500:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty500.yaml
    index_ticker: "NIFTY:500"
    annualization_factor: 250
  nifty_next_50:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_next_50.yaml
    index_ticker: "NIFTY:NEXT50"
    annualization_factor: 250
  nifty200:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty200.yaml
    index_ticker: "NIFTY:200"
    annualization_factor: 250
  nifty_midcap_50:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_midcap_50.yaml
    index_ticker: "NIFTY:MIDCAP50"
    annualization_factor: 250
  nifty_midcap_100:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_midcap_100.yaml
    index_ticker: "NIFTY:MIDCAP100"
    annualization_factor: 250
  nifty_smallcap_50:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_smallcap_50.yaml
    index_ticker: "NIFTY:SMALLCAP50"
    annualization_factor: 250
  nifty_smallcap_100:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_smallcap_100.yaml
    index_ticker: "NIFTY:SMALLCAP100"
    annualization_factor: 250
  nifty_smallcap_250:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_smallcap_250.yaml
    index_ticker: "NIFTY:SMALLCAP250"
    annualization_factor: 250
  nifty_microcap_250:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_microcap_250.yaml
    index_ticker: "NIFTY:MICROCAP250"
    annualization_factor: 250
  nifty_midsmallcap_400:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_midsmallcap_400.yaml
    index_ticker: "NIFTY:MIDSMALLCAP400"
    annualization_factor: 250
  nifty_largemidcap_250:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_largemidcap_250.yaml
    index_ticker: "NIFTY:LARGEMIDCAP250"
    annualization_factor: 250
  nifty_total_market:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_total_market.yaml
    index_ticker: "NIFTY:TOTALMARKET"
    annualization_factor: 250
  # --- NSE Sectoral indices (15) -------------------------------------------
  # Materialized by scripts/data_pipelines/sectoral_fetch.py (PR for task #97).
  # Constituent CSVs sourced from archives.nseindia.com; benchmark index
  # tickers track the symbols already cached under nse_equities.
  nifty_bank:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_bank.yaml
    index_ticker: "NIFTY:BANK"
    annualization_factor: 250
  nifty_it:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_it.yaml
    index_ticker: "NIFTY:IT"
    annualization_factor: 250
  nifty_auto:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_auto.yaml
    index_ticker: "NIFTY:AUTO"
    annualization_factor: 250
  nifty_pharma:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_pharma.yaml
    index_ticker: "NIFTY:PHARMA"
    annualization_factor: 250
  nifty_fmcg:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_fmcg.yaml
    index_ticker: "NIFTY:FMCG"
    annualization_factor: 250
  nifty_metal:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_metal.yaml
    index_ticker: "NIFTY:METAL"
    annualization_factor: 250
  nifty_realty:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_realty.yaml
    index_ticker: "NIFTY:REALTY"
    annualization_factor: 250
  nifty_energy:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_energy.yaml
    index_ticker: "NIFTY:ENERGY"
    annualization_factor: 250
  nifty_media:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_media.yaml
    index_ticker: "NIFTY:MEDIA"
    annualization_factor: 250
  nifty_psu_bank:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_psu_bank.yaml
    index_ticker: "NIFTY:PSUBANK"
    annualization_factor: 250
  nifty_private_bank:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_private_bank.yaml
    index_ticker: "NIFTY:PVTBANK"
    annualization_factor: 250
  nifty_financial_services:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_financial_services.yaml
    index_ticker: "NIFTY:FINSVC"
    annualization_factor: 250
  nifty_healthcare:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_healthcare.yaml
    index_ticker: "NIFTY:HEALTHCARE"
    annualization_factor: 250
  nifty_consumer_durables:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_consumer_durables.yaml
    index_ticker: "NIFTY:CONSUMERDURABLES"
    annualization_factor: 250
  nifty_oil_and_gas:
    source: configs/data_pipelines/domains/nse_equities/universe_nifty_oil_and_gas.yaml
    index_ticker: "NIFTY:OILGAS"
    annualization_factor: 250
  # --- US equities ---------------------------------------------------------
  sp500:
    source: configs/data_pipelines/domains/us_equities/universe_sp500.yaml
    index_ticker: "INDEX:^SPX"
    annualization_factor: 252
  nasdaq100:
    source: configs/data_pipelines/domains/us_equities/universe_nasdaq100.yaml
    index_ticker: "INDEX:^NDX"
    annualization_factor: 252
  russell1000:
    source: configs/data_pipelines/domains/us_equities/universe_russell1000.yaml
    index_ticker: "INDEX:^RUT"
    annualization_factor: 252

# ---------------------------------------------------------------------------
# Walk-forward split. Per-stock row budget: train + val + eval + test = 1600.
# Tickers below min_rows_per_ticker are dropped from the panel.
# ---------------------------------------------------------------------------
split:
  train_rows: 800                    # where the model fits
  val_rows: 400                      # where CatBoost early-stops + where Spiegelhalter Z is run
  eval_rows: 200                     # held-out segment; headline metrics computed here
  test_rows: 100                     # final sanity-check segment beyond eval
  n_folds: 1                         # single-fold default; specs can override
  min_rows_per_ticker: 1600          # JIOFIN (578) and MAXHEALTH (1322) excluded by this

# ---------------------------------------------------------------------------
# Features. Pool = all 279 candidate columns across 16 families (see
# V1_PLAN.md Stage 2). The agent prunes per-iteration inside the FS+HP loop.
# ---------------------------------------------------------------------------
features:
  candidates: all                    # "all" = 279 cols across F1..F16
  exclude: []                        # spec can list explicit names or globs
  lookback_windows: [5, 10, 20, 50, 100, 200]

# ---------------------------------------------------------------------------
# Backend config. v1 = catboost only.
# ---------------------------------------------------------------------------
backend:
  library: catboost                  # v1.1 may add lightgbm / xgboost behind same contract

  # Calibration policy. Default = conditional isotonic gated by Spiegelhalter Z.
  # Options: native | conditional_isotonic | isotonic_always | platt
  # (see docs/gbdt/EXPERIMENT_SPEC.md § Calibration).
  calibration_method: conditional_isotonic
  calibration_z_threshold: 2.0       # |z| < this -> ship native; else apply isotonic

  # FS+HP loop config. Agent reads diagnostic bundle per iteration; bounded
  # by max_iterations; inner-stops on plateau or degradation.
  fs_hp_loop:
    max_iterations: 8                # hard cap
    plateau_threshold: 0.005         # absolute val Brier improvement floor over last 2 iter
    degradation_gate: 0.01           # 1% multiplicative degrade from best-seen val Brier -> stop

  # Iteration-0 HP starting values. The agent starts here and explores
  # bounded by the ranges in CATBOOST_HP_REFERENCE.md per parameter.
  # Per-experiment specs override individual keys via backend.hp_starting.
  hp_starting:
    iterations: 1000
    learning_rate: 0.05              # explicit value; otherwise CatBoost auto-derives
    depth: 6
    l2_leaf_reg: 3.0
    min_data_in_leaf: 1
    rsm: 1.0
    bootstrap_type: Bayesian
    bagging_temperature: 1.0
    random_strength: 1.0
    boosting_type: Ordered           # ordered boosting; ~2-3x slower than Plain but better on small data
    early_stopping_rounds: 75
    # auto_class_weights left unset by default; per-cell guidance in CATBOOST_HP_REFERENCE.md § 5

  # Pinned HPs (never overridable from a spec; see V1_PLAN.md Stage 4).
  hp_pinned:
    has_time: true                   # mandatory for walk-forward correctness
    loss_function: Logloss
    eval_metric: BrierScore
    custom_metric: [Logloss, BrierScore, AUC]

# ---------------------------------------------------------------------------
# Global seed. Pinned across CatBoost, NumPy, and any sampling in the
# diagnostic bundle generator. Sweep only for explicit seed-variance work.
# ---------------------------------------------------------------------------
random_seed: 42

# ---------------------------------------------------------------------------
# Artifact roots.
# ---------------------------------------------------------------------------
artifacts:
  experiment_dir: results/gbdt/experiments    # checked-in per-experiment artifact dirs
  runs_dir: runs/gbdt                          # gitignored raw per-iter tempfiles
  headline_data_dir: results/gbdt/data         # per [[project-results-layout]]
