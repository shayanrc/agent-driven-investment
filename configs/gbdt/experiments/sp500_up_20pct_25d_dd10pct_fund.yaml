# Experiment: sp500_up_20pct_25d_dd10pct_fund
#
# Fundamentals-augmented variant (F1–F16 + F18 valuation ratios) — A/B partner
# of ..._base_v2. IDENTICAL config except features.candidates: all_fundamentals.
# The only difference vs the baseline is the ~9 point-in-time valuation-ratio
# columns, so any test R-Precision@K delta isolates the F18 contribution.
# max_iterations: 1 (single fit) keeps HP matched to base_v2 — clean A/B
# discipline. Plan: V1.7. Memo: _272.
target:
  universe: sp500
  direction: up
  threshold_pct: 20
  horizon_days: 25
  max_drawdown: 0.10

split:
  mode: date_aligned
  train_start: 2019-01-01

features:
  candidates: all_fundamentals   # F1–F16 + F18 (fundamentals); 'all' untouched

backend:
  library: xgboost
  calibration_method: conditional_isotonic
  fs_hp_loop:
    callback_mode: default
    max_iterations: 1
    plateau_threshold: 0.005
    degradation_gate: 0.01

random_seed: 42
