# Experiment: nasdaq100_up_50pct_200d_dd25pct
#
# Cell: nasdaq100 UP 50% in 200 trading days with max drawdown 25%.
#
# Historical base rate (non-overlapping blocks from EDA scan 2026-05-26,
# nasdaq100 panel, 1990-01-02 to 2026-05-22): 12.574%  (404 positives
# / 3213 blocks).  Priority rank: #14 of 57 in the
# US sweep.
#
# Sweep-mode params: fs_hp_loop.max_iterations=3. This is a SCREENING
# run; cells that surface meaningful calibration improvement get re-run
# with the full 8-iter loop later.
#
# Schema reference: docs/gbdt/EXPERIMENT_SPEC.md.

target:
  universe: nasdaq100
  direction: up
  threshold_pct: 50
  horizon_days: 200
  max_drawdown: 0.25          # numeric, e.g. 0.05 not "5%"

backend:
  calibration_method: conditional_isotonic
  fs_hp_loop:
    max_iterations: 3
    plateau_threshold: 0.005
    degradation_gate: 0.01
